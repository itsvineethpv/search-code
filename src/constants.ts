export enum Inputs {
  searchString = 'search-string',
  fileNamePattern = 'file-name-pattern',
  filePathPattern = 'file-path-pattern',
  organization = 'organization',
  user = 'user',
  repository = 'repository',
  token = 'token',
  notifyTo = 'notifyTo',
  language = 'language',
  extension = 'extension'
}
